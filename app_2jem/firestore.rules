rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ALLOW EVERYTHING FOR NOW (To test if this fixes the error)
      allow read, write: if true;
    }
  }
}